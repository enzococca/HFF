'''Created on 30 05 2021@author: Enzo Cocca'''from builtins import objectfrom sqlalchemy import Table, Column, Integer, String, Text, MetaData, create_engine, UniqueConstraintfrom geoalchemy2 import Geometryfrom ..hff_system__conn_strings import Connectionfrom ...utility.hff_system__OS_utility import Hff_OS_Utilityclass anchor_point_table(object):    # connection string postgres"    internal_connection = Connection()    # create engine and metadata    engine = create_engine(internal_connection.conn_str(), echo=False, convert_unicode=True)    metadata = MetaData()    # define tables    anchor_point = Table('anchor_point', metadata,                       Column('gid', Integer, primary_key=True),                       Column('the_geom', String),                       Column('site', Text),                       Column('code', Text),                       Column('years', Integer),                       Column('link', Text),                       Column('type', Text),                       Column('obj', Text),                    UniqueConstraint('gid')                       )    metadata.create_all(engine)