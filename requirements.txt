# HFF plugin dependencies.
# Use >= (not ==) so the resolver picks the highest version compatible with
# the QGIS-bundled Python (3.9 through 3.12+).
# CRITICAL: numpy must be exactly 1.26.4 for QGIS compatibility (numpy 2.x
# breaks QGIS). Commented out because QGIS already ships a compatible numpy.
#numpy==1.26.4

# Core DB
SQLAlchemy>=2.0.0
SQLAlchemy-Utils>=0.41.0
GeoAlchemy2>=0.15.0

# Reports & data
reportlab>=4.0.0
XlsxWriter>=3.1.0
openpyxl>=3.1.0
matplotlib>=3.7.0
pandas>=2.0.0
requests>=2.28.0

# Documents & media
PyMuPDF>=1.23.0
python-docx>=1.0.0
opencv-python>=4.8.0  # Optional - may already be installed or cause issues on Windows

# Utilities
psutil>=5.9.0
nltk>=3.8.0

# AI / LLM
openai>=1.0.0

# 3D visualization
pygame>=2.5.0
pyvista>=0.43.0
pyvistaqt>=0.11.0
vtk>=9.2.0
