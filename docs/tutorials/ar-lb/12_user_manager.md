# إدارة المستخدمين

يشرح هذا الدليل كيفية إدارة المستخدمين والصلاحيات في HFF Survey للبيئات متعددة المستخدمين.

## نظرة عامة

يتضمن HFF نظام إدارة مستخدمين لقواعد بيانات PostgreSQL يتيح:

- وصول عدة مستخدمين إلى نفس قاعدة البيانات
- صلاحيات مبنية على الأدوار
- سجلات تدقيق لمن أنشأ/عدّل السجلات
- التحكم في الوصول للبيانات الحساسة

> **ملاحظة**: إدارة المستخدمين متاحة فقط مع قواعد بيانات PostgreSQL. قواعد بيانات SQLite لا تدعم الوصول متعدد المستخدمين.

---

## فتح إدارة المستخدمين

1. انقر على **قائمة HFF > الإعدادات > إدارة المستخدمين**
2. أو انقر على أيقونة **إدارة المستخدمين** ![مستخدمون](icons/iconConn.png) في شريط الأدوات

---

## أدوار المستخدمين

يدعم HFF ثلاثة أدوار مدمجة:

| الدور | الوصف | الصلاحيات |
|-------|-------|-----------|
| **مدير** | وصول كامل للنظام | إنشاء/حذف المستخدمين، جميع عمليات البيانات، التكوين |
| **محرر** | إدارة البيانات | إنشاء، قراءة، تحديث، حذف السجلات |
| **مشاهد** | وصول للقراءة فقط | عرض السجلات فقط، بدون تعديلات |

---

## تسجيل الدخول

عند تمكين إدارة المستخدمين:

1. يظهر مربع حوار تسجيل الدخول عند فتح نماذج HFF
2. أدخل **اسم المستخدم** و**كلمة المرور**
3. انقر **موافق** للمصادقة
4. يحدد دورك الإجراءات المتاحة

---

## إدارة المستخدمين (للمدير فقط)

### إنشاء مستخدم جديد

1. افتح مربع حوار إدارة المستخدمين
2. انقر زر **إضافة مستخدم**
3. املأ تفاصيل المستخدم:
   - **اسم المستخدم**: اسم دخول فريد
   - **كلمة المرور**: كلمة مرور آمنة
   - **البريد الإلكتروني**: بريد الاتصال (اختياري)
   - **الدور**: حدد مدير أو محرر أو مشاهد
4. انقر **حفظ**

### تعديل مستخدم

1. حدد المستخدم من القائمة
2. انقر زر **تعديل**
3. عدّل التفاصيل حسب الحاجة
4. انقر **حفظ**

### حذف مستخدم

1. حدد المستخدم من القائمة
2. انقر زر **حذف**
3. أكد الحذف

> **تحذير**: حذف المستخدم لا يحذف سجلاته. تبقى السجلات منسوبة للمستخدم المحذوف.

---

## تغيير كلمة المرور

1. افتح مربع حوار إدارة المستخدمين
2. اذهب إلى علامة **الملف الشخصي**
3. أدخل كلمة المرور الحالية
4. أدخل كلمة المرور الجديدة
5. أكد كلمة المرور الجديدة
6. انقر **تغيير كلمة المرور**

---

## تفاصيل الصلاحيات

### صلاحيات المدير

- إنشاء وتعديل وحذف المستخدمين
- الوصول إلى جميع النماذج والسجلات
- تصدير جميع البيانات
- تعديل التكوين
- نسخ احتياطي/استعادة قاعدة البيانات

### صلاحيات المحرر

- إنشاء سجلات جديدة
- تعديل السجلات الخاصة
- تعديل السجلات (إذا سمح المدير)
- تصدير البيانات
- عرض جميع السجلات

### صلاحيات المشاهد

- عرض جميع السجلات
- تصدير البيانات (للقراءة فقط)
- لا يمكن إنشاء أو تعديل السجلات

---

## إدارة الجلسات

### إعدادات المهلة

يمكن للمديرين تكوين:

- **مهلة الجلسة**: تسجيل خروج تلقائي بعد عدم النشاط
- **تذكرني**: خيار البقاء متصلاً

### الجلسات النشطة

عرض وإدارة الجلسات النشطة:

1. اذهب إلى علامة **الجلسات**
2. شاهد جميع المستخدمين المتصلين حالياً
3. يمكن للمديرين فرض تسجيل خروج مستخدمين آخرين

---

## سجل التدقيق

عند التمكين، يتتبع HFF:

- من أنشأ كل سجل
- من عدّل كل سجل آخر مرة
- طابع وقت التغييرات

### عرض معلومات التدقيق

يُظهر كل سجل:

- **أنشأه**: اسم مستخدم المُنشئ
- **تاريخ الإنشاء**: متى تم إنشاء السجل
- **عدّله**: اسم مستخدم آخر مُعدِّل
- **تاريخ التعديل**: متى تم تغيير السجل آخر مرة

---

## تمكين إدارة المستخدمين

### الإعداد الأولي

1. اتصل بقاعدة بيانات PostgreSQL
2. افتح إدارة المستخدمين
3. انقر **تهيئة نظام المستخدمين**
4. أنشئ أول مستخدم مدير
5. مكّن مصادقة المستخدمين

### خيارات التكوين

| الخيار | الوصف |
|--------|-------|
| **تسجيل دخول مطلوب** | فرض المصادقة لجميع النماذج |
| **السماح بالتسجيل** | السماح للمستخدمين بالتسجيل الذاتي (موافقة المدير) |
| **سياسة كلمة المرور** | الحد الأدنى للطول، متطلبات التعقيد |
| **مدة الجلسة** | كم تبقى الجلسات نشطة |

---

## أفضل الممارسات

### الأمان

- استخدم كلمات مرور قوية وفريدة
- لا تشارك بيانات اعتماد المدير
- راجع قائمة المستخدمين بانتظام
- احذف المستخدمين غير النشطين

### التنظيم

- أنشئ مستخدمين لكل عضو في الفريق
- أسند أدنى الصلاحيات المطلوبة
- استخدم أسماء مستخدمين وصفية
- وثّق من لديه وصول المدير

---

## استكشاف الأخطاء وإصلاحها

### تعذر تسجيل الدخول

- تحقق من تهجئة اسم المستخدم
- تحقق من Caps Lock
- اتصل بالمدير لإعادة تعيين كلمة المرور

### الصلاحية مرفوضة

- تحقق من أن دورك لديه الصلاحيات المطلوبة
- اتصل بالمدير لترقية الوصول

### إدارة المستخدمين غير متاحة

- تأكد من أنك تستخدم PostgreSQL
- تحقق من اتصال قاعدة البيانات
- تحقق من أن لديك صلاحيات المدير

---

*التالي: [نموذج حفظ الفخار](13_pottery_conservation_form.md)*
